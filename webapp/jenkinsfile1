pipeline {
    agent any

    stages {
        stage('Building Docker image') {
            steps {
                sh 'cd webapp && docker build -f Dockerfile -t img-frontend .'
            }
        }
    }
}
